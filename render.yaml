services:
  - type: web
    name: url-shortener
    env: node
    region: oregon
    plan: free
    rootDir: .
    buildCommand: npm ci && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
